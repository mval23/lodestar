# Configuration for the end-to-end build only. These are not real: the tests
# stub this host at the network boundary, so nothing is ever sent to it.
# Committed on purpose, so CI builds the same app the tests expect.
VITE_SUPABASE_URL=https://e2e-test.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_e2e_test_placeholder
